# Dream Cycle Ledger

Durable cross-night memory for the `ruvnet/ruClip` nightly Dream Machine cycle.
One row per run, appended only, never rewritten.

| Date | Deep | Finding | Issue | PR | Evaluated? | Verdict | Effect | Witness | Prior-night fates |
|---|---|---|---|---|---|---|---|---|---|
| 2026-09-02 | architecture | `agentdb-adapter.ts` (1417 lines, 8 bounded contexts) is a God-module; extracted the operating-budget circuit breaker into `store/operating-budget.ts` (re-exported, zero call-site changes) | [#8](https://github.com/ruvnet/ruClip/issues/8) | [#9](https://github.com/ruvnet/ruClip/pull/9) | yes | ACCEPT | -116 lines in the god-module, 320/320 tests unchanged, 0 other files touched | `f0a6bef15b18f29dd17e0266e316025401f13780532c61a63dcd51670e3f9a14` | n/a (first run) |
| 2026-09-03 | performance | `checkOperatingBudget` (Gate 2, runs on every heartbeat fire) fetched N tracked session-cost keys via N sequential `memory_retrieve` round trips; replaced with a bounded worker-pool fan-out (`mapWithConcurrency`, cap 25) — output unchanged, empirically proven sequential-vs-concurrent both ways (parent fails the new concurrency regression test, candidate passes) | [#10](https://github.com/ruvnet/ruClip/issues/10) | [#11](https://github.com/ruvnet/ruClip/pull/11) | yes | ACCEPT | 155ms→32ms measured (N=6, mocked 25ms/call, 4.8x); synthetic N=200 sweep 1600ms→67ms at cap 25 (23.9x); 320→321 tests, 0 regressions | `144a11f1fd079317b6caed4a2974d5305b8fd766989ea2567f37fb493263f81d` | #8/PR#9: OPEN, still unmerged (0 of last 2 dream-cycle PRs merged as of tonight — bias toward small, reviewable candidates applied) |
| 2026-09-05 | correctness | `wouldCreateCycle`/`graphNeighbors` (`agentdb-adapter.ts`) parsed the real `agentdb_graph-query` MCP tool's k-hop response as `{nodes:[{id}]}`; the real, exact-pinned tool (`@claude-flow/cli@3.38.20`, confirmed in this repo's own `node_modules`) returns `{results:[{nodeId}]}` — cycle prevention and `getChildIssueIds`/`getBlockerIssueIds` have been silent no-ops against the real bridge since written. Fixed the field parsing; corrected every test mock (5 call sites, 4 files) from the same wrong fictional shape to match. A first hypothesis (widen the hardcoded `depth: 5` k-hop probe) was implemented, passed 323/323, then REJECTED by an independent critic who read the real tool source and found `depth` is server-clamped to `complexityBudget.maxDepth` (default 5) regardless — a clean rejection that led directly to tonight's actual finding | [#12](https://github.com/ruvnet/ruClip/issues/12) | [#13](https://github.com/ruvnet/ruClip/pull/13) | yes | ACCEPT | Baseline (mocks corrected, code unfixed) 316/320 (4 genuine behavioral failures); candidate 320/320, 0 regressions, `harness:bench-verify` hash unchanged | `00af13527b925842621f8252b5faea310ae7750fd4062f990ac66915195a03a3` | #8/PR#9 and #10/PR#11: both still OPEN/unmerged (2026-09-04 developer-experience night appears to have not run at all — no branch/issue/PR found; not investigable further from inside this session, flagged for the routine owner) |
