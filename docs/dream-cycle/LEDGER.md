# Dream Cycle Ledger

Durable cross-night memory for the `ruvnet/ruClip` nightly Dream Machine cycle.
One row per run, appended only, never rewritten.

| Date | Deep | Finding | Issue | PR | Evaluated? | Verdict | Effect | Witness | Prior-night fates |
|---|---|---|---|---|---|---|---|---|---|
| 2026-09-02 | architecture | `agentdb-adapter.ts` (1417 lines, 8 bounded contexts) is a God-module; extracted the operating-budget circuit breaker into `store/operating-budget.ts` (re-exported, zero call-site changes) | [#8](https://github.com/ruvnet/ruClip/issues/8) | [#9](https://github.com/ruvnet/ruClip/pull/9) | yes | ACCEPT | -116 lines in the god-module, 320/320 tests unchanged, 0 other files touched | `f0a6bef15b18f29dd17e0266e316025401f13780532c61a63dcd51670e3f9a14` | n/a (first run) |
| 2026-09-03 | performance | `checkOperatingBudget` (Gate 2, runs on every heartbeat fire) fetched N tracked session-cost keys via N sequential `memory_retrieve` round trips; replaced with a bounded worker-pool fan-out (`mapWithConcurrency`, cap 25) — output unchanged, empirically proven sequential-vs-concurrent both ways (parent fails the new concurrency regression test, candidate passes) | [#10](https://github.com/ruvnet/ruClip/issues/10) | [#11](https://github.com/ruvnet/ruClip/pull/11) | yes | ACCEPT | 155ms→32ms measured (N=6, mocked 25ms/call, 4.8x); synthetic N=200 sweep 1600ms→67ms at cap 25 (23.9x); 320→321 tests, 0 regressions | `144a11f1fd079317b6caed4a2974d5305b8fd766989ea2567f37fb493263f81d` | #8/PR#9: OPEN, still unmerged (0 of last 2 dream-cycle PRs merged as of tonight — bias toward small, reviewable candidates applied) |
| 2026-09-05 | correctness | `wouldCreateCycle`/`graphNeighbors` (`agentdb-adapter.ts`) parsed the real `agentdb_graph-query` MCP tool's k-hop response as `{nodes:[{id}]}`; the real, exact-pinned tool (`@claude-flow/cli@3.38.20`, confirmed in this repo's own `node_modules`) returns `{results:[{nodeId}]}` — cycle prevention and `getChildIssueIds`/`getBlockerIssueIds` have been silent no-ops against the real bridge since written. Fixed the field parsing; corrected every test mock (5 call sites, 4 files) from the same wrong fictional shape to match. A first hypothesis (widen the hardcoded `depth: 5` k-hop probe) was implemented, passed 323/323, then REJECTED by an independent critic who read the real tool source and found `depth` is server-clamped to `complexityBudget.maxDepth` (default 5) regardless — a clean rejection that led directly to tonight's actual finding | [#12](https://github.com/ruvnet/ruClip/issues/12) | [#13](https://github.com/ruvnet/ruClip/pull/13) | yes | ACCEPT | Baseline (mocks corrected, code unfixed) 316/320 (4 genuine behavioral failures); candidate 320/320, 0 regressions, `harness:bench-verify` hash unchanged | `00af13527b925842621f8252b5faea310ae7750fd4062f990ac66915195a03a3` | #8/PR#9 and #10/PR#11: both still OPEN/unmerged (2026-09-04 developer-experience night appears to have not run at all — no branch/issue/PR found; not investigable further from inside this session, flagged for the routine owner) |
| 2026-09-03 | performance | `checkOperatingBudget` (Gate 2, runs on every heartbeat fire) fetched N tracked session-cost keys via N sequential `memory_retrieve` round trips; replaced with a bounded worker-pool fan-out (`mapWithConcurrency`, cap 25) — output unchanged, empirically proven sequential-vs-concurrent both ways (parent fails the new concurrency regression test, candidate passes) | LOCAL (no `gh`/gist tool this session — see report) | LOCAL (see `docs/dream-cycle/2026-09-03-performance-report.md`; branch `dream/2026-09-03-performance` not yet pushed as of this row) | yes | ACCEPT | 155ms→32ms measured (N=6, mocked 25ms/call, 4.8x); synthetic N=200 sweep 1600ms→67ms at cap 25 (23.9x); 320→321 tests, 0 regressions | `144a11f1fd079317b6caed4a2974d5305b8fd766989ea2567f37fb493263f81d` | #8/PR#9: OPEN, still unmerged (0 of last 2 dream-cycle PRs merged as of tonight — bias toward small, reviewable candidates applied) |
| 2026-09-06 | security | `verifyActorCredential`/`verifyHumanIdentityAttestation` (`actor-credential.ts`/`human-identity-attestation.ts`) guarded their single-use nonce with a separate `memory_retrieve` existence check + `memory_store` write — a CWE-367 TOCTOU race: two concurrent verifications of the same credential/attestation could both observe "not yet used" and both succeed, defeating the single-use guarantee. Fixed by collapsing each guard into one atomic `memory_store` call with `upsert: false`, verified (against the real installed `@claude-flow/cli@3.38.20` source, both its `better-sqlite3` bridge and `sql.js` fallback write paths) to strict-insert against a real `UNIQUE(namespace, key)` constraint. Also scanned: 39 `npm audit` findings (1 critical, 14 high) in `ruflo`'s transitive tree, none reachable from ruClip's own code — flagged as a next step, not tonight's candidate | [#14](https://github.com/ruvnet/ruClip/issues/14) | [#15](https://github.com/ruvnet/ruClip/pull/15) | yes | ACCEPT | Baseline (guard unfixed) 320/323 (3 predicted failures: concurrency race reproduced deterministically twice via a synchronization-barrier mock, plus one `upsert:false` assertion); candidate 323/323, 0 regressions | `dfb958d95f850fabb61b5f7812646ebdf367966a337530fcc02fd5e740368e88` | #8/PR#9 (2026-09-02): still OPEN, unmerged. #10/PR#11 (2026-09-03): CLOSED, unmerged. #12/PR#13 (2026-09-05): CLOSED, unmerged. 0 of the last 3 dream-cycle PRs merged as of tonight — bias toward a tiny, single-conceptual-change candidate applied (fix touches 2 files of production code, ~160 lines total incl. tests) |
| 2026-09-07 | architecture | `agentdb-adapter.ts` god-module: extracted the pattern-store section (`RuclipPatternNamespace`/`storePattern`/`searchPatterns`/`PatternSearchResult`, ~36 lines) into a new `store/pattern-store.ts`, re-exported unchanged — same extract-and-re-export technique as PR #9. Backfilled here (2026-09-11, this row was carried on `dream/2026-09-07-architecture` but never reached `main`, since PR #17 branched from the same unmerged `main` tip PR #9 did) | [#16](https://github.com/ruvnet/ruClip/issues/16) | [#17](https://github.com/ruvnet/ruClip/pull/17) | yes | ACCEPT | Baseline (parent `5c3ea0d`) 321/321; candidate 321/321, 0 regressions; `agentdb-adapter.ts` 1483→1459 lines (net -24), new `pattern-store.ts` +50 lines | `42457282a4acff8840c09fb049dcde96909e2ddd939751cf49adea2c807912c1` | 0 of #9/#11/#13/#15 (4 prior dream-cycle PRs) merged as of that night — biased toward the smallest remaining self-contained extraction |
| 2026-09-08 | performance | 3 structurally identical sequential `for...of` tier-scan loops (`working`+`episodic`) in `listIssuesForGoal`/`listApprovalTransitionsForCompany`/`listHeartbeatsForCompany` (`agentdb-adapter.ts`) replaced with `Promise.all` fan-out — the 2026-09-03 report's claim that `checkOperatingBudget` was the *only* sequential per-item RPC loop in this file was incomplete; same conceptual fix applied uniformly to the 3 it missed. Backfilled here (2026-09-11) for the same reason as the 09-07 row above | [#18](https://github.com/ruvnet/ruClip/issues/18) | [#19](https://github.com/ruvnet/ruClip/pull/19) | yes | ACCEPT | Baseline (parent `6e73a8f`) 327/327; baseline+new tests pre-fix 3/3 fail (proves the tests assert real behavior); candidate 330/330, `tsc --strict` clean; synthetic 25ms/tier mock ~50ms→~25ms per call; `harness:bench-verify` hash unchanged | `c37ec041d7726ab46e3fac01b5069ee03ae8d774ad69d2d32a3d115aca803285` | 0 of #9/#11/#13/#15/#17 (5 prior dream-cycle PRs) merged as of that night — same mechanical-fix bias applied |
| 2026-09-09 | developer-experience | `npm run lint` was a permanent no-op stub, never wired into CI. Wired a new `tsconfig.lint.json` (`noUnusedLocals`/`noUnusedParameters`/`noFallthroughCasesInSwitch`/`noImplicitReturns`, zero new deps) into `npm run lint` + CI — surfaced 3 real findings, one non-cosmetic: `propose-budget-mutation.ts`'s `buildMutation(parent, newThreshold)` computed the tightened Autogenous `hardStopThreshold` and silently dropped it (ADR-103 audit-trail gap). Fixed all 3 findings, added `AutogenousMutationRecord.proposedHardStopThreshold`. Backfilled here (2026-09-11) for the same reason as the two rows above | [#20](https://github.com/ruvnet/ruClip/issues/20) | [#21](https://github.com/ruvnet/ruClip/pull/21) | yes | ACCEPT | Baseline (gate wired, findings unfixed) 3 lint errors/exit 2; candidate 0 lint errors/exit 0; 327/327 tests unchanged (2 existing tests gained assertions); synthetic-regression probe (injected 4th unused var) correctly caught then reverted; adversarial hardcoded-wrong-value probe correctly failed 2 tests then restored; `harness:bench-verify` hash unchanged; independent critic verdict CLEAR | `0ed42ee494f35df7af4883a126b327169174afc8532e8527ab57ed67381282d2` | 0 of #9/#11/#13/#15/#17/#19 (6 prior dream-cycle PRs) merged as of that night — biased toward the tiniest reviewable diff |
| 2026-09-10 | correctness | `graphNeighbors` (`agentdb-adapter.ts`, shared k-hop helper behind `getChildIssueIds`/`getBlockerIssueIds`) never excluded the queried node's own id from its own neighbor list. The real, currently-installed native graph backend this deployment actually loads (`@ruvector/graph-node@2.1.0`, confirmed load-bearing via `npm ls`) includes the start node itself in `kHopNeighbors(nodeId, k)`'s result — reproduced live against the real installed native binary, independently re-reproduced by a fresh adversarial critic with its own script. Every issue was silently reporting itself as its own child and its own blocker. Fixed with a one-line filter (`id !== nodeId`); 2 existing tests corrected to the real mock shape, 2 new regression tests added | [#22](https://github.com/ruvnet/ruClip/issues/22) | [#23](https://github.com/ruvnet/ruClip/pull/23) | yes | ACCEPT | Baseline (tests updated to real mock shape, code unfixed) 325/329 (4 genuine failures); candidate 329/329, 0 regressions; `harness:bench-verify` hash unchanged (`840fd8d2d698…`); independent critic re-reproduced the defect from scratch, verdict CLEAR | `e701f0245f003d32e12d8a44b431f645ee2414157e8a152bd5dad9acacc495a5` | 0 of #9/#11/#13/#15/#17/#19/#21 (7 prior dream-cycle PRs) merged as of tonight — bias toward a tiny, single-conceptual-change candidate applied (1 production file, 1 filter clause, ~52/-4 lines incl. tests) |
| 2026-09-11 | security | `checkAuthorizationGuard` (Guard C, `agentdb-adapter.ts`, used by `persistIssue`) accepts `{ actor: OrgMember }` as an "already-verified" authorization shape (threaded in by `applyApprovalTransition`), but for that shape trusted the caller-supplied `actor` object instead of `persistedActor` (the ground-truth record it already recalls two lines later) for two checks: the `companyId` cross-tenant guard (skipped entirely, gated behind `'credential' in authorization`) and the closing `verifyActorHoldsClaim` call (queried using the caller's `kind`/`role`, not the persisted ones). Same bug class two prior rounds in this exact function already closed for other fields ("round 6" status, "round 7" companyId-for-credential-shape) — this is "round 8," closing the two fields those didn't cover. Unreachable via the only production caller (`applyApprovalTransition` already resolves+company-matches its own actor first) — bounded severity, same posture as the 2026-09-02 cross-tenant claims-collision fix — but `persistIssue` is exported specifically so a future already-verified caller can present `{ actor }`. Independently found by a parallel research subagent too. Also backfilled this ledger's missing 2026-09-07/08/09/10 rows (above) | [#24](https://github.com/ruvnet/ruClip/issues/24) | [#25](https://github.com/ruvnet/ruClip/pull/25) | yes | ACCEPT | Baseline (Guard C unfixed, new tests present) 327/329 (2 predicted failures: a genuine cross-tenant claims bypass succeeding; a legitimate actor wrongly rejected due to a caller-supplied stale role); candidate 329/329, 0 regressions; `tsc --strict` clean; `harness:bench-verify` hash unchanged (`840fd8d2d698…`); independent critic re-derived the fix cold, re-ran both receipts itself, verdict CLEAR | `0f6893ca44ea811a2928538217b5a492af9bca7219664ea87df7348de2fc8fa9` | #9 (09-02) OPEN unmerged. #11 (09-03) CLOSED unmerged. #13 (09-05) CLOSED unmerged. #15 (09-06) **MERGED**. #17 (09-07) OPEN unmerged. #19 (09-08) OPEN unmerged. #21 (09-09) OPEN unmerged. #23 (09-10) OPEN unmerged, draft, no CI configured. 1 of the last 8 dream-cycle PRs merged as of tonight — biased toward a small, surgical, 2-line production diff |
