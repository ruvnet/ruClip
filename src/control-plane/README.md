# Control plane (placeholder)

Phase 1 target per `docs/PLAN.md`: a thin Company → Goals → Issues
(parent/child, blockers, single-assignee) schema layered on AgentDB, plus
budget-gated heartbeats (`ruflo-loop-workers` + `ruflo-cost-tracker`) and
approval gates (`claims_handoff` / `claims_accept-handoff`).

No implementation yet — this file is a placeholder so the directory is
tracked in git before Phase 1 work lands.
